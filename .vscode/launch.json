{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "Launch",
        "program": "${workspaceFolder}/target/debug/sl",
        "args": ["examples/hello-world.js"],
        "cwd": "${workspaceFolder}"
    }
    ]
}