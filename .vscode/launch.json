{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "Launch",
        "program": "${workspaceFolder}/target/debug/test262-runner",
        "args": ["run","-vvv"],
        "cwd": "${workspaceFolder}"
    }
    ]
}