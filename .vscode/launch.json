{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "Launch",
        "program": "${workspaceFolder}/target/debug/prop",
        "args": ["examples/hello-world.js"],
        "cwd": "${workspaceFolder}"
    }
    ]
}